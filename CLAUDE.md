@AGENTS.md

This file is Claude Code's view into the project's behavioral contract. It is intentionally a thin shim: the canonical contract is `AGENTS.md`, which every AI agent (Claude Code, Codex, Gemini, Hydra-spawned workers) is expected to read at session start. If `CLAUDE.md` and `AGENTS.md` ever drift, `AGENTS.md` wins.
