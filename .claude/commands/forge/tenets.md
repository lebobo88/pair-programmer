---
description: Forge crown — read or amend the project's tenets. Mythic alias for /pp:constitution (CONSTITUTION.md, the Immortal Head).
argument-hint: [status|scaffold|amend]
---

> _The tenets are the Immortal Head._ One head that cannot die — the rest of the harness's heads (engineer, architect, judges, sentinel, seal-bearer) regenerate and evolve, but the tenets do not. The harness reads them, hashes them, and refuses to release against drift. Amendments are HITL-only.

Run `/pp:constitution $ARGUMENTS`. The behavior is identical to `/pp:constitution`. Read `.claude/commands/pp/constitution.md` for the canonical contract.
