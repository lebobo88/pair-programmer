# Verification: USER_GUIDE.md (full document)

You are reviewing the user-facing guide for the `pair-programmer` harness. The guide is at `docs/USER_GUIDE.md` (relative to repo root `<repo-root>`). Read it now from disk via your standard tools, then audit.

## Audit questions (be terse — name the issue + section)

1. **Factual accuracy.** Spot-check the document's numeric claims (16 commands, 28 agents, 15 teams, 10 profiles, 13 rubrics, 10 forums, 20 missability checks, 25 hooks, 46 MCP tools, 5 design templates, 6 gate types — wait, I claim 7) against the actual files in:
   - `.claude/commands/pp/*.md`
   - `.claude/agents/*.md`
   - `.claude/teams/*.yaml`
   - `.claude/profiles/*.yaml`
   - `.claude/rubrics/index.md`
   - `.claude/settings.json` (hooks)
   - `daemon/src/orchestrator/missability.ts` (20 checks)
   - `daemon/src/orchestrator/forums.ts` (10 forums)
   - `daemon/src/orchestrator/design-templates.ts` (5 templates)
   - `daemon/src/mcp/harness-server.ts` (42 tools), `codex-server.ts` (2), `gemini-server.ts` (2)
   - `daemon/src/orchestrator/gates.ts` (gate types)

   Report any number that's wrong.

2. **Inline accuracy.** Pick 3 specific commands and verify the doc's command signature + prereqs match the actual `.claude/commands/pp/*.md` body. Pick `feature-team`, `bug-fix-team`, and `web-ui` (profile) and verify the tabular summaries match the YAML.

3. **Coverage gaps.** What user-invocable capabilities or operator concerns are MISSING from the guide entirely? Be specific. Don't list things that are out of scope (the plan said the guide consolidates all user-facing surfaces — code internals are out of scope).

4. **Reader-friendliness regressions.** A first-time reader should be able to go install → first run → a working /pp:run with this doc alone. Where would they get stuck or confused?

5. **Cross-link integrity.** The guide links to several files (e.g. `daemon/src/...`, `docs/...`, `.claude/...`). Spot-check 5 of those paths exist.

Score each on 0..1 (factual_accuracy, coverage, reader_friendliness, link_integrity). Pass = all ≥0.7.

Lead with the worst issues. Aim under 600 words.
